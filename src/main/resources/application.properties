# ===============================
# GenBridge Application Configuration
# ===============================

# Application Info
spring.application.name=genbridge-backend
server.port=8080

# ===============================
# Database Configuration (Supabase PostgreSQL)
# ===============================
# spring.datasource.url=jdbc:postgresql://localhost:5432/genbridge
# spring.datasource.username=postgres
# spring.datasource.password=password
# spring.datasource.driver-class-name=org.postgresql.Driver
# Use your Supabase session pooler credentials below
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres
spring.datasource.username=postgres.jczcbtykmfzzktfghzzx
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# JWT Configuration
# ===============================
# TODO: Change this to a secure secret key in production (at least 256 bits)
jwt.secret=genbridge-secret-key-change-this-in-production-make-it-at-least-256-bits-long-for-security
jwt.expiration=86400000

# JWT expiration time in milliseconds (86400000 = 24 hours)

# ===============================
# Logging Configuration
# ===============================
logging.level.org.springframework.security=DEBUG
logging.level.com.genbridge.backend=DEBUG

# ===============================
# CORS Configuration (for React frontend)
# ===============================
# Will be configured in Java config class

# ===============================
# Swagger/OpenAPI Configuration
# ===============================
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true


spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1